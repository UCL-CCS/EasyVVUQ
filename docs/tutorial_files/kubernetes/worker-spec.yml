kind: Pod
metadata:
  labels:
    foo: bar
spec:
  restartPolicy: Never
  containers:
  - image: orbitfold/easyvvuq:latest
    imagePullPolicy: IfNotPresent
    args: [dask-worker, --nthreads, '3', --no-dashboard, --memory-limit, 6GB, --death-timeout, '60']
    name: easyvvuq
    resources:
      limits:
        cpu: "3"
        memory: 6G
      requests:
        cpu: "3"
        memory: 6G